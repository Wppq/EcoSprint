openapi: 3.1.0
info:
  license:
    name: Private
    url: https://ecosprint.my.id
  title: Eco Sprint
  version: 1.0.0
servers:
  - url: $domainRoot

x-tagGroups:
  - name: Resources
    tags:
      - User
      - Donation

paths:
  /v1/user:
    $ref: user/index.yaml
  /v1/user/register:
    $ref: user/regiterUser.yaml 
    
  /v1/donation:
    $ref: donation/index.yaml